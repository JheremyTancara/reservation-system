const CrearReserva = () => {
  return (
    <div className="min-h-screen flex items-center justify-center bg-amber-50">
      <div className="bg-white rounded-xl shadow p-8 max-w-lg w-full space-y-4 text-center">
        <h1 className="text-3xl font-bold text-gray-900">Crear Reserva</h1>
        <p className="text-gray-600">
          Esta pantalla aún no tiene el formulario conectado, pero el componente
          ya existe para que puedas avanzar con el resto del flujo.
        </p>
      </div>
    </div>
  );
};

export default CrearReserva;


